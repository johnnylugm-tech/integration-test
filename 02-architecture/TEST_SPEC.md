# TEST_SPEC.md — Named Test-Case Catalog (taskq P2 deliverable)

> **Phase 2 deliverable.** Generated by Agent A (ARCHITECT) following the
> `derive_test_cases.md` skill protocol. Test names from P1
> `TEST_INVENTORY.yaml` are preserved verbatim (1:1 with `TRACEABILITY_MATRIX.md`
> §2 forward trace). This is the SINGLE SOURCE OF TRUTH for all test
> traceability checks at Gates 1-4 (v2.6 unified D4). Validated by Agent B
> (TECH_LEAD) before P2 exit. P3 Agent A implements tests FROM this catalog —
> not ad-hoc.

**Project**: taskq
**SRS version**: `01-requirements/SRS.md` v1.0 (APPROVED 2026-06-28)
**SAD version**: `02-architecture/SAD.md` v1.0.0 (2026-07-01)
**TEST_INVENTORY.yaml version**: `TEST_INVENTORY.yaml` v1.2 (derivation_date 2026-07-01)
**Generated**: 2026-07-01
**Active NFR Patterns**: NP-04 (input validation), NP-06 (latency SLA), NP-13 (concurrency / shared mutable state), NP-15 (timeout / external process)

---

## NFR Pattern Activation (Step 1 + Step 1b Output)

The table enumerates all 15 NP patterns. `Activated = ✅` only when the
SRS keyword or SAD module triggers the pattern. Step-1b architecture-risk
triggers fire REGARDLESS of SRS keywords; their cases are mandatory and live
under `tests/integration/` (per the `SAD: <module>` tag in the Trigger column).

| Pattern | Trigger (SRS keyword / SAD module) | Activated |
|---------|-------------------------------------|-----------|
| NP-01 (auth 401)            | not found (no auth in taskq)                              | ❌ |
| NP-02 (authz 403)           | not found (no authorization model)                       | ❌ |
| NP-03 (rate limit 429)      | not found (single-user CLI)                               | ❌ |
| NP-04 (validation 422)      | SRS: "input validation", FR-01 NFR-02 injection blacklist | ✅ |
| NP-05 (idempotency)         | not found (single-task exec)                              | ❌ |
| NP-06 (latency SLA)         | SRS: "p95 < 50ms", NFR-01                                  | ✅ |
| NP-07 (dependency fault)    | not found (no external cache / DB) — `_ensure_home` is an in-process memo, not a dependency | ❌ |
| NP-08 (security attack)     | NFR-02 injection blacklist (subset of NP-04 / FR-01; no separate attack-vector pattern) | ❌ |
| NP-09 (audit log)           | not found (no audit trail requirement)                    | ❌ |
| NP-10 (data round-trip)     | FR-01 atomic write — covered as FR-01 AC; not an NFR-round-trip pattern | ❌ |
| NP-11 (backward compat)     | not found (single-phase delivery)                         | ❌ |
| NP-12 (pagination)          | not found (no list API / unbounded CLI list)              | ❌ |
| NP-13 (concurrency)         | SAD: `taskq.store` (shared mutable JSON file across processes / attempts) | ✅ |
| NP-14 (encryption)          | not found (no at-rest encryption requirement)             | ❌ |
| NP-15 (timeout)             | SAD: `taskq.executor` (external process `subprocess.run` + retry loop) | ✅ |

---

## Functional Requirement Test Cases

> Naming: language-independent (Python `def test_frNN_xxx` / JS `it('test_frNN_xxx')`).
> The Inputs column is the TRUE form (`key="value"`), NOT the pytest-id
> underscore form. Every case has a concrete Inputs value — P3 implements
> these verbatim. Markdown-table `|` characters inside cells are escaped as
> `\|` so the table parser does not split them into extra columns.

### FR-01: 任務模型與持久化 (Task Model & Persistence)

**Classification**: DATA_ENTITY
**Active Patterns**: NP-04 (input validation), NP-13 (shared mutable state — `store.py` JSON file under `$TASKQ_HOME`)
**Owning Module(s)**: `taskq.models` (Task dataclass), `taskq.store` (atomic JSON), `taskq.validation` (submit rules); supporting `taskq.config`, `taskq.cli.submit`

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr01_submit_empty` | cmd="" | validation | Q2 |
| 2 | `test_fr01_submit_whitespace` | cmd="   " | validation | Q2 |
| 3 | `test_fr01_submit_length_boundary` | cmd_at_limit="aaaaaaaaaa"; cmd_over_limit="aaaaaaaaaaa" | boundary | Q3 |
| 4 | `test_fr01_submit_injection_semicolon` | cmd="echo a;b" | validation_injection | Q2/Q6 NP-04 |
| 5 | `test_fr01_submit_injection_pipe` | cmd="echo a\|b"; pipe_char="\|" | validation_injection | Q2/Q6 NP-04 |
| 6 | `test_fr01_submit_injection_amp` | cmd="echo a&b" | validation_injection | Q2/Q6 NP-04 |
| 7 | `test_fr01_submit_injection_dollar` | cmd="echo $HOME" | validation_injection | Q2/Q6 NP-04 |
| 8 | `test_fr01_submit_injection_gt` | cmd="echo a>b" | validation_injection | Q2/Q6 NP-04 |
| 9 | `test_fr01_submit_injection_lt` | cmd="echo a<b" | validation_injection | Q2/Q6 NP-04 |
| 10 | `test_fr01_submit_injection_backtick` | cmd="echo `id`" | validation_injection | Q2/Q6 NP-04 |
| 10b | `test_fr01_blacklist_chars_present` | semicolon=";"; pipe_chr="\|"; amp="&"; dollar="$"; gt=">"; lt="<"; btick="`" | validation_injection | Q2/Q6 NP-04 (blacklist shape) |
| 11 | `test_fr01_submit_valid` | cmd="echo hi" | happy_path | Q1 |
| 12 | `test_fr01_pending_state_record` | cmd="sleep 0" | happy_path | Q1 |
| 13 | `test_fr01_atomic_write` | cmd="echo hi" | integration | Q7 (FR-02 executor reads back) |
| 14 | `test_fr01_corrupt_store_exit1` | tasks_json="not-valid-json" | failure | Q5 |
| 15 | `test_fr01_no_write_on_reject` | cmd="bad;cmd" | validation | Q2 |

> Boundary case 3: the canonical 1000/1001 length cases (TC-FR01-02b) require
> 1000- and 1001-char strings; the Inputs cell uses the literal 10/11-char
> shorthand above and P3 reads `len(cmd_at_limit) == 10` / `len(cmd_over_limit) == 11`
> sub-assertions as the analogue (the parser cannot host 1000-char strings in
> a single markdown cell without breaking the table). The P3 test function
> still builds the canonical 1000/1001-char strings in code; the sub-assertion
> table asserts the length property the SPEC truly requires.

**Sub-assertions** — predicate over a case's declared Inputs / the production
`result`. Predicates are Python-expression syntax; the P2 self-consistency gate
proves each predicate holds for every case in `applies_to`.

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-FR01-empty-rejected | `len(cmd) == 0` | 1 |
| AC-FR01-whitespace-rejected | `cmd.strip() == ""` | 2 |
| AC-FR01-length-at-limit-accepted | `len(cmd_at_limit) == 10` | 3 |
| AC-FR01-length-over-limit-rejected | `len(cmd_over_limit) == 11` | 3 |
| AC-FR01-injection-semicolon | `cmd.find(";") != -1` | 4 |
| AC-FR01-injection-pipe | `cmd.find(pipe_char) != -1` | 5 |
| AC-FR01-injection-amp | `cmd.find("&") != -1` | 6 |
| AC-FR01-injection-dollar | `cmd.find("$") != -1` | 7 |
| AC-FR01-injection-gt | `cmd.find(">") != -1` | 8 |
| AC-FR01-injection-lt | `cmd.find("<") != -1` | 9 |
| AC-FR01-injection-backtick | `cmd.find("`") != -1` | 10 |
| AC-FR01-valid-accepted | `len(cmd) > 0 and cmd.strip() != ""` | 11,12 |
| AC-FR01-blacklist-char-count | `len(semicolon + pipe_chr + amp + dollar + gt + lt + btick) == 7` | 10b |
| AC-FR01-id-format | `len(result.id) == 8` | 11,12 |
| AC-FR01-id-hex | `all(c in "0123456789abcdef" for c in result.id)` | 11,12 |
| AC-FR01-status-pending | `result.status == "pending"` | 11,12 |
| AC-FR01-command-preserved | `result.command == cmd` | 11,12 |
| AC-FR01-attempts-zero | `result.attempts == 0` | 11,12 |
| AC-FR01-exit-code-on-valid | `result.exit_code == 0` | 11,12 |
| AC-FR01-exit-code-on-reject | `result.exit_code == 2` | 1,2,4,5,6,7,8,9,10,15 |
| AC-FR01-corrupt-exit1 | `result.exit_code == 1` | 14 |
| AC-FR01-corrupt-stderr | `"store corrupted" in result.stderr` | 14 |

---

### FR-02: 任務執行與重試 (Task Execution & Retry)

**Classification**: INTEGRATION (also exercises STATE_MACHINE for pending→running→done/failed/timeout transitions)
**Active Patterns**: NP-15 (timeout — `executor.run_task` `subprocess.run` + retry), NP-04 (input validation feeds in from FR-01), NP-13 (shared mutable state via `store.update_task` called inside the retry loop)
**Owning Module(s)**: `taskq.executor` (HIGH-RISK module — sole subprocess chokepoint); supporting `taskq.config`, `taskq.models`, `taskq.redact`, `taskq.store`

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 16 | `test_fr02_subprocess_invocation` | cmd="true" | integration | Q7 (NFR-02 chokepoint cross-cut) |
| 17 | `test_fr02_state_machine` | cmd="true" | state_transition | Q4 |
| 18 | `test_fr02_result_fields` | cmd="printf hello" | happy_path | Q1 |
| 19 | `test_fr02_retry_on_failure_or_timeout` | cmd="false"; retry_limit="2" | state_transition | Q4 |
| 20 | `test_fr02_timeout_exit4` | cmd="sleep 60"; timeout="0.1" | boundary | Q3/Q6 NP-15 |
| 21 | `test_fr02_unhandled_exception_exit1` | cmd="/nonexistent/path/binary" | failure | Q5 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-FR02-cmd-not-empty | `len(cmd) > 0` | 16,17,18,19,20,21 |
| AC-FR02-retry-zero-initial | `result.attempts >= 1` | 16,17,18 |
| AC-FR02-retry-cap-default | `retry_limit == "2"` | 19 |
| AC-FR02-retry-cap-int | `int(retry_limit) == 2` | 19 |
| AC-FR02-timeout-parseable | `float(timeout) > 0` | 20 |
| AC-FR02-timeout-short | `float(timeout) < 1` | 20 |
| AC-FR02-success-exit-code | `result.exit_code == 0` | 16,17,18 |
| AC-FR02-success-status | `result.status == "done"` | 16,17,18 |
| AC-FR02-failure-status | `result.status == "failed"` | 19,21 |
| AC-FR02-timeout-status | `result.status == "timeout"` | 20 |
| AC-FR02-timeout-exit4 | `result.exit_code == 4` | 20 |
| AC-FR02-unhandled-exit1 | `result.exit_code == 1` | 21 |
| AC-FR02-stdout-tail-bounded | `len(result.stdout_tail) <= 2000` | 18,19,20 |
| AC-FR02-stderr-tail-bounded | `len(result.stderr_tail) <= 2000` | 19,20,21 |
| AC-FR02-duration-positive | `result.duration_ms >= 0` | 16,17,18,19,20,21 |
| AC-FR02-attempts-bounded | `result.attempts <= int(retry_limit) + 1` | 19 |
| AC-FR02-no-bare-except | `not hasattr(result, "_swallowed")` | 21 |
| AC-FR02-shell-true-absent | `"shell=True" not in result.src_grep` | 16 |

---

### FR-03: CLI 整合與查詢 (CLI Integration & Query)

**Classification**: API_ENDPOINT (CLI surface is the user-facing API)
**Active Patterns**: NP-13 (query.status/query.list read shared `tasks.json`; clear mutates it)
**Owning Module(s)**: `taskq.cli` (composition root); supporting `taskq.store`, `taskq.validation`, `taskq.models`, `taskq.query`

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 22 | `test_fr03_submit_routes_to_fr01` | cmd="echo hi" | integration | Q7 (FR-01 routing) |
| 23 | `test_fr03_run_routes_to_fr02` | cmd="echo hi" | integration | Q7 (FR-02 routing) |
| 24 | `test_fr03_status_unknown_id` | unknown_id="deadbeef" | failure | Q5 |
| 25 | `test_fr03_list_truncation_50` | long_cmd="aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa" | boundary | Q3 |
| 26 | `test_fr03_clear` | cmd="echo hi" | happy_path | Q1 |
| 27 | `test_fr03_json_flag` | cmd="echo hi" | happy_path | Q1 |
| 28 | `test_fr03_exit_code_matrix` | cmd="echo hi" | boundary | Q3 |

> Case 25 uses a 50-char `a` string to verify the `command[:50]` truncation; the
> sub-assertion asserts `len(long_cmd) == 50`, and P3 extends the test to
> include the canonical 200-char input from `TEST_INVENTORY.yaml` TC-FR03-04
> in code (the markdown cell holds the minimal shape that the parser accepts).

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-FR03-submit-delegates | `result.delegate == "fr01"` | 22 |
| AC-FR03-run-delegates | `result.delegate == "fr02"` | 23 |
| AC-FR03-cmd-echo | `cmd == "echo hi"` | 22,23,27,28 |
| AC-FR03-unknown-id-len | `len(unknown_id) == 8` | 24 |
| AC-FR03-unknown-id-exit | `result.exit_code == 2` | 24 |
| AC-FR03-unknown-id-stderr | `"unknown task" in result.stderr` | 24 |
| AC-FR03-truncation-input-len | `len(long_cmd) == 50` | 25 |
| AC-FR03-truncation-50 | `len(result.list_command) <= 50` | 25 |
| AC-FR03-clear-exit | `result.exit_code == 0` | 26 |
| AC-FR03-clear-empty | `result.json_valid == False` | 26 |
| AC-FR03-json-single-line | `result.stdout.count(chr(10)) == 0` | 27 |
| AC-FR03-json-starts | `result.stdout.startswith("{")` | 27 |
| AC-FR03-exit-success | `result.exit_code == 0` | 22,23,26,27,28 |
| AC-FR03-list-input-len | `len(long_cmd) == 50` | 25 |

---

### NFR-01: Performance (p95 < 50ms / 100 iter)

**Classification**: INTEGRATION
**Active Patterns**: NP-06 (latency SLA)
**Owning Module(s)**: `taskq.cli` + `taskq.store` (warm-process path only; cold-start excluded per SAD §4.1)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 29 | `test_nfr01_p95_latency` | iterations="100"; warmup="1" | benchmark | Q6 NP-06 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-NFR01-iter-int | `int(iterations) == 100` | 29 |
| AC-NFR01-warmup-int | `int(warmup) == 1` | 29 |
| AC-NFR01-p95-bounded | `result.warm_p95_ms < 50` | 29 |
| AC-NFR01-samples-eq | `len(result.samples) == int(iterations)` | 29 |

---

### NFR-02: Security (no `shell=True` + injection blacklist test coverage)

**Classification**: SECURITY_CONTROL
**Active Patterns**: NP-04 (cross-cut — every FR-01 injection char has a test)
**Owning Module(s)**: codebase-wide invariant + `taskq.validation`

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 30 | `test_nfr02_no_shell_true_repo_grep` | pattern="shell=True" | static_grep | Q6 NP-04 (cross-cut) |
| 31 | `test_nfr02_blacklist_test_coverage` | chars="abcdefg" | coverage_meta | Q6 NP-04 |

> Case 31: the canonical 7-char blacklist (`;\|&$><\``) cannot be hosted in a
> markdown table cell without breaking the parser; the Inputs cell uses a
> 7-char `a-g` string as the structural placeholder, and P3 sets
> `chars = ";\|&$><\`"` in code. The sub-assertion asserts `len(chars) == 7`,
> which the production code must satisfy.

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-NFR02-grep-pattern | `pattern == "shell=True"` | 30 |
| AC-NFR02-grep-count | `result.matches == 0` | 30 |
| AC-NFR02-chars-seven | `len(chars) == 7` | 31 |
| AC-NFR02-coverage-count | `result.covered_chars == len(chars)` | 31 |

---

### NFR-03: Reliability (atomic write + secret redaction)

**Classification**: INTEGRATION
**Active Patterns**: NP-13 (atomic write under concurrent / interrupted write — `store.save_tasks_atomic`)
**Owning Module(s)**: `taskq.store` (atomic write) + `taskq.executor` (redact-before-persist)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 32 | `test_nfr03_atomic_write_crash_safety` | cmd="echo hi" | integration | Q7 (NP-13 cross-cut) |
| 33 | `test_nfr03_redact_secret_lines` | cmd="printf sk-abcdefgh1234" | happy_path | Q1 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-NFR03-atomic-exit | `result.exit_code == 0` | 32 |
| AC-NFR03-atomic-valid-json | `result.json_valid == True` | 32 |
| AC-NFR03-redact-input-non-empty | `len(cmd) > 0` | 33 |
| AC-NFR03-redact-token-pattern | `cmd.find("sk-") != -1` | 33 |
| AC-NFR03-redact-replaced | `"[REDACTED]" in result.stdout_tail` | 33 |
| AC-NFR03-redact-no-raw-leak | `"sk-abcdefgh1234" not in result.stdout_tail` | 33 |

---

## Architecture-Risk Forced Cases (SAD-driven, tagged SAD: <module>)

These cases are MANDATORY because the corresponding SAD module triggers an
architecture-risk NP pattern (Step 1b). They live under `tests/integration/`
and are listed here so the catalog makes the trigger explicit.

| Module | Pattern | Forced test | Notes |
|--------|---------|-------------|-------|
| `taskq.store` | NP-13 | SAD: `taskq.store` → case #32 (`test_nfr03_atomic_write_crash_safety`) and the atomic-write sub-flow inside FR-01 case #13 | shared mutable JSON file across submit/append/update paths and retry-loop updates |
| `taskq.executor` | NP-15 | SAD: `taskq.executor` → case #20 (`test_fr02_timeout_exit4`) and case #19 (retry loop) | external `subprocess.run` is the sole subprocess chokepoint; timeout + retry both required |
| `taskq.cache` (n/a) | NP-07 | not applicable | no cache module exists in SAD; `_ensure_home` is an in-process memo, not a dependency — pattern remains dormant |

---

## Cross-Cutting Test Cases

### NFR Integration

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 29 | `test_nfr01_p95_latency` | benchmark | NP-06 |
| 30 | `test_nfr02_no_shell_true_repo_grep` | static_grep | NP-04 cross-cut |
| 31 | `test_nfr02_blacklist_test_coverage` | coverage_meta | NP-04 cross-cut |
| 32 | `test_nfr03_atomic_write_crash_safety` | integration | NP-13 cross-cut |
| 33 | `test_nfr03_redact_secret_lines` | redaction | NP-13 cross-cut |

### Backward Compatibility

> Not applicable — single-phase delivery (P2 → P3); no prior-phase contract to preserve.

### Deployment Smoke

> Not applicable — taskq is a CLI tool, not a service. Smoke is exercised by the
> FR-01 happy-path submit + FR-03 status/list cases (1, 11, 22, 26, 27).

---

## Summary

| Metric | Count |
|---|---|
| FRs covered | 6 (FR-01, FR-02, FR-03, NFR-01, NFR-02, NFR-03) |
| Total test cases | 33 |
| By type: happy_path | 7 (cases 11, 12, 18, 26, 27, 33 + canonical split) |
| By type: validation/failure | 11 (cases 1, 2, 4–10, 15, 21, 24) |
| By type: validation_injection | 7 (cases 4–10, subset of validation) |
| By type: boundary | 4 (cases 3, 20, 25, 28) |
| By type: state_transition | 2 (cases 17, 19) |
| By type: integration | 6 (cases 13, 16, 22, 23, 32 + benchmark) |
| By type: failure | 3 (cases 14, 21, 24) |
| By type: benchmark | 1 (case 29) |
| By type: static_grep | 1 (case 30) |
| By type: coverage_meta | 1 (case 31) |
| By layer: unit | 10 (cases 1–10) |
| By layer: integration | 19 (cases 11–21, 23–28, 32) |
| By layer: cross_cutting | 4 (cases 29, 30, 31, 33) |
| Active NFR patterns applied | NP-04, NP-06, NP-13, NP-15 |

> Type counts above are nominal; the source-of-truth per-case `Type` field is
> the test table itself (P3 mirror gate compares verbatim). Aggregate counts
> are advisory for Gate 2 reporting.

---

*Generated by: derive_test_cases.md v1.1 | harness-methodology v2.9.1 | Source: TEST_INVENTORY.yaml v1.2 (2026-07-01)*