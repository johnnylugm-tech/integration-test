# TEST_SPEC.md — taskq Named Test-Case Catalog (Phase 2 SSOT)

<!-- harness:template-stub REMOVED: Round 1 fill -->

> **Phase 2 deliverable.** Generated by Agent A (ARCHITECT) following the
> `derive_test_cases.md` skill protocol (8-Question per-FR + Step 1b SAD-driven
> architecture-risk triggers). Test names from P1 `TEST_INVENTORY.yaml` are
> preserved verbatim where specified — D4 spec-coverage matches by name. This
> file is the SINGLE SOURCE OF TRUTH for all test traceability at Gates 1-4
> (v2.6 unified D4). Validated by Agent B (TECH_LEAD) before P2 exit. P3 Agent A
> implements tests FROM this catalog — never ad-hoc.

**Project**: taskq
**SPEC version**: SPEC.md v3.0.0 (2026-07-04)
**SAD version**: 02-architecture/SAD.md v1.0 (2026-07-04)
**TEST_INVENTORY.yaml version**: TEST_INVENTORY.yaml v1.1 (2026-07-04)
**Generated**: 2026-07-04
**Active NFR Patterns**: NP-04, NP-06, NP-07, NP-13, NP-15

---

## NFR Pattern Activation (Step 1 + Step 1b Output)

| Pattern | Trigger (SRS keyword / SAD module) | Activated |
|---------|-------------------------------------|-----------|
| NP-01 (auth 401)           | not found                                                       | ❌ |
| NP-02 (authz 403)          | not found                                                       | ❌ |
| NP-03 (rate limit 429)     | not found                                                       | ❌ |
| NP-04 (validation 422)     | SRS §3 FR-01: "exit 2 + stderr" validation rules                | ✅ |
| NP-05 (idempotency)        | not found                                                       | ❌ |
| NP-06 (latency SLA)        | SRS §4 NFR-01: "p95 < 50ms / 100 iter"                          | ✅ |
| NP-07 (dependency fault)   | SAD §2.5.6 `cache.py` (TTL cache.json persistence)              | ✅ |
| NP-08 (security attack)    | not found                                                       | ❌ |
| NP-09 (audit log)          | not found                                                       | ❌ |
| NP-10 (data round-trip)    | not found                                                       | ❌ |
| NP-11 (backward compat)    | not found (single-version CLI; no schema evolution)             | ❌ |
| NP-12 (pagination)         | not found                                                       | ❌ |
| NP-13 (concurrency)        | SAD §2.5.3 `store.py` (threading.Lock + tasks.json I/O)         | ✅ |
| NP-14 (encryption)         | not found                                                       | ❌ |
| NP-15 (timeout)            | SAD §2.5.4 `executor.py` (subprocess.run timeout)               | ✅ |

Step 1b architecture-risk triggers (forced regardless of SRS keywords):
- **NP-13** forced by SAD: `store.py` shared mutable state (threading.Lock + tasks.json/breaker.json/cache.json atomic writes).
- **NP-15** forced by SAD: `executor.py` external subprocess (subprocess.run with timeout).
- **NP-07** forced by SAD: `cache.py` cache file (cache.json TTL persistence — analog to "dependency fault" — file I/O fault domain).

Forced cases are tagged **SAD:** and live under `tests/integration/` (per skill protocol).

---

## Functional Requirement Test Cases

### FR-01: Task submit & input validation (5 ACs)

**Classification**: DATA_ENTITY
**Active Patterns**: NP-04

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr01_add_task_success_atomic_write` | command="echo hi"; name="t1"; expected_id_len="8"; expected_status="pending" | happy_path | Q1 |
| 2 | `test_fr01_add_task_empty_rejected` | command=""; name=""; expected_exit="2" | validation | Q2 |
| 3 | `test_fr01_add_task_whitespace_rejected` | command="   "; name=""; expected_exit="2" | validation | Q2 |
| 4 | `test_fr01_add_task_too_long_rejected` | command=<1001-char 'x' literal>; name=""; expected_exit="2"; command_len=1001 | boundary | Q3 |
| 5 | `test_fr01_add_task_injection_chars_rejected` | command="echo hi; rm x"; name=""; expected_exit="2" | validation | Q2 |
| 6 | `test_fr01_add_task_name_conflict_rejected` | command="echo ok"; name="dup"; expected_exit="2" | validation | Q2 |

Note: TEST_INVENTORY.yaml names preserved verbatim. Case 5 covers the 6 injection chars (`; | & $ > < ` `) — single test function parametrically enumerates them.

**Sub-assertions** — predicates over Inputs / production `result`. Python-expression syntax per spec.

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-FR01-01-id-len | `len(result_id) == 8` | 1 |
| AC-FR01-01-id-hex | `all(c in "0123456789abcdef" for c in result_id)` | 1 |
| AC-FR01-01-status-pending | `result_status == "pending"` | 1 |
| AC-FR01-01-task-count-one | `result_tasks_count == 1` | 1 |
| AC-FR01-02-empty-rejected | `expected_exit == "2"` | 2 |
| AC-FR01-03-whitespace-rejected | `expected_exit == "2"` | 3 |
| AC-FR01-04-too-long-rejected | `expected_exit == "2"` | 4 |
| AC-FR01-04-len-1001 | `command_len == 1001` | 4 |
| AC-FR01-05-injection-rejected | `expected_exit == "2"` | 5 |
| AC-FR01-05-has-semicolon | `";" in command` | 5 |
| AC-FR01-05-via-NFR02-cross-ref | `";" in command and expected_exit == "2"` | 5 |
| AC-FR01-06-name-conflict-rejected | `expected_exit == "2"` | 6 |
| AC-FR01-06-name-dup | `name == "dup"` | 6 |

---

### FR-02: Task executor & status state-machine (5 ACs)

**Classification**: STATE_MACHINE
**Active Patterns**: NP-13, NP-15

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr02_subprocess_shlex_split_no_shell_true` | command="echo hi"; expected_exit="0"; expected_shell="false" | happy_path | Q1 |
| 2 | `test_fr02_status_machine_done_failed_timeout` | commands="echo hi,false,sleep 5"; timeout="1.0"; statuses="done,failed,timeout" | happy_path | Q4 (state_transition) |
| 3 | `test_fr02_result_fields_tail_2000` | command="printf 'a%.0s' {1..3000}"; tail_len=2000 | happy_path | Q1 |
| 4 | `test_fr02_concurrent_threadpool` | n_tasks="10"; max_workers="4"; expected_done_count="10" | unit (Q5 SAD-context) | Q5 |
| 5 | `test_fr02_timeout_exit_code_4` | command="sleep 5"; timeout="1.0"; expected_exit="4"; expected_status="timeout" | nfr_pattern (NP-15) | Q6 |
| 6 | `test_fr02_concurrent_run_all_no_loss` | n_tasks="20"; max_workers="4"; expected_tasks_json_valid="true"; expected_loss_count="0" | integration (SAD: store.py NP-13) | Q5 |

Note: TEST_INVENTORY names preserved. Case 6 integration variant under `tests/integration/` (NP-13 SAD-driven, TC-FR02-04b). Case 4 (TC-FR02-04a) is unit-layer per TEST_INVENTORY.

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-FR02-01-exit-0 | `expected_exit == "0"` | 1 |
| AC-FR02-01-no-shell-true | `expected_shell == "false"` | 1 |
| AC-FR02-02-done | `"done" in statuses` | 2 |
| AC-FR02-02-failed | `"failed" in statuses` | 2 |
| AC-FR02-02-timeout | `"timeout" in statuses` | 2 |
| AC-FR02-03-tail-2000 | `tail_len == 2000` | 3 |
| AC-FR02-04-pool-done | `expected_done_count == n_tasks` | 4 |
| AC-FR02-04-pool-workers | `max_workers == "4"` | 4 |
| AC-FR02-05-timeout-exit-4 | `expected_exit == "4"` | 5 |
| AC-FR02-05-timeout-status | `expected_status == "timeout"` | 5 |
| AC-FR02-06-json-valid | `expected_tasks_json_valid == "true"` | 6 |
| AC-FR02-06-no-loss | `expected_loss_count == "0"` | 6 |
| AC-FR02-06-n-tasks | `n_tasks == "20"` | 6 |

---

### FR-03: Retry + circuit breaker (5 ACs, 8 tests)

**Classification**: STATE_MACHINE
**Active Patterns**: NP-13, NP-15

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr03_exponential_backoff_injectable_sleep` | backoff_base="0.1"; attempt_n="3"; expected_sleep="0.8" | happy_path | Q4 |
| 2 | `test_fr03_retry_limit_cap` | retry_limit="2"; attempts_executed="3"; expected_final_status="failed" | boundary | Q3 |
| 3 | `test_fr03_timeout_triggers_retry` | command="sleep 5"; timeout="1.0"; retry_limit="1"; expected_attempts="2" | state_transition | Q4 |
| 4 | `test_fr03_threshold_opens_breaker` | threshold="3"; final_failures="3"; expected_state="OPEN" | state_transition | Q4 |
| 5 | `test_fr03_open_refuses_with_exit_3` | state="OPEN"; expected_exit="3"; expected_stderr_contains="breaker open" | validation | Q2 |
| 6 | `test_fr03_half_open_probe_success_closes` | state="HALF_OPEN"; probe_outcome="success"; expected_state="CLOSED"; expected_count="0" | state_transition | Q4 |
| 7 | `test_fr03_half_open_probe_failure_reopens` | state="HALF_OPEN"; probe_outcome="failure"; expected_state="OPEN" | state_transition | Q4 |
| 8 | `test_fr03_state_persisted_atomically` | state="CLOSED"; file="breaker.json"; expected_json_valid="true" | unit (Q5 SAD-context) | Q5 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-FR03-01-backoff-formula | `expected_sleep == "0.8"` | 1 |
| AC-FR03-01-backoff-base | `backoff_base == "0.1"` | 1 |
| AC-FR03-01-attempt-n | `attempt_n == "3"` | 1 |
| AC-FR03-02-cap-applied | `expected_final_status == "failed"` | 2 |
| AC-FR03-02-limit-2 | `retry_limit == "2"` | 2 |
| AC-FR03-03-timeout-retry | `expected_attempts == "2"` | 3 |
| AC-FR03-04-opens | `expected_state == "OPEN"` | 4 |
| AC-FR03-04-threshold-3 | `threshold == "3"` | 4 |
| AC-FR03-05-exit-3 | `expected_exit == "3"` | 5 |
| AC-FR03-05-stderr-marker | `expected_stderr_contains == "breaker open"` | 5 |
| AC-FR03-06-closes | `expected_state == "CLOSED"` | 6 |
| AC-FR03-06-count-zero | `expected_count == "0"` | 6 |
| AC-FR03-07-reopens | `expected_state == "OPEN"` | 7 |
| AC-FR03-08-json-valid | `expected_json_valid == "true"` | 8 |
| AC-FR03-08-file | `file == "breaker.json"` | 8 |

---

### FR-04: TTL result cache (4 ACs)

**Classification**: ALGORITHM
**Active Patterns**: NP-07

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr04_signature_sha256` | command="echo hi"; expected_sig_len="64" | happy_path | Q1 |
| 2 | `test_fr04_cached_replay_no_subprocess` | command="echo hi"; cached_at_offset="0"; ttl="3600"; expected_status="done"; expected_cached="true" | happy_path | Q1 |
| 3 | `test_fr04_expiry_normal_execution` | command="echo hi"; cached_at_offset="7200"; ttl="3600"; expected_status="done"; expected_cached="false" | boundary | Q3 |
| 4 | `test_fr04_atomic_thread_safe_write` | n_writers="8"; command="echo hi"; expected_cache_json_valid="true"; expected_loss_count="0" | unit (Q5 SAD-context) | Q5 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-FR04-01-sha256-len-64 | `expected_sig_len == "64"` | 1 |
| AC-FR04-02-replay-status | `expected_status == "done"` | 2 |
| AC-FR04-02-cached-true | `expected_cached == "true"` | 2 |
| AC-FR04-02-ttl-3600 | `ttl == "3600"` | 2 |
| AC-FR04-03-expiry-reruns | `expected_status == "done"` | 3 |
| AC-FR04-03-cached-false | `expected_cached == "false"` | 3 |
| AC-FR04-03-expired | `cached_at_offset == "7200"` | 3 |
| AC-FR04-04-json-valid | `expected_cache_json_valid == "true"` | 4 |
| AC-FR04-04-no-loss | `expected_loss_count == "0"` | 4 |
| AC-FR04-04-writers | `n_writers == "8"` | 4 |

---

### FR-05: CLI integration (3 ACs, 4 tests)

**Classification**: API_ENDPOINT
**Active Patterns**: NP-04

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr05_argparse_subcommands` | commands="submit,run,status,list,clear"; expected_rcs="0,0,0,0,0" | integration | Q7 |
| 2 | `test_fr05_json_flag_round_trip` | command="submit echo hi --json"; expected_json_key="id" | integration | Q7 |
| 3 | `test_fr05_exit_code_matrix` | cases="submit '' (2), submit 'echo hi; rm' (2), run unknown (2), run timeout (4), run breaker-open (3), success (0)"; expected_rcs="2,2,2,4,3,0" | integration | Q7 |
| 4 | `test_fr05_unknown_task_id_exit_2` | task_id="deadbeef"; expected_exit="2"; expected_stderr_contains="unknown task" | integration | Q7 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-FR05-01-5-subcommands | `commands == "submit,run,status,list,clear"` | 1 |
| AC-FR05-01-rcs-zero | `expected_rcs == "0,0,0,0,0"` | 1 |
| AC-FR05-02-json-key | `expected_json_key == "id"` | 2 |
| AC-FR05-02-json-flag | `"--json" in command` | 2 |
| AC-FR05-03-rc-matrix | `expected_rcs == "2,2,2,4,3,0"` | 3 |
| AC-FR05-03-six-cases | `cases == "submit '' (2), submit 'echo hi; rm' (2), run unknown (2), run timeout (4), run breaker-open (3), success (0)"` | 3 |
| AC-FR05-04-unknown-id | `expected_exit == "2"` | 4 |
| AC-FR05-04-stderr-marker | `expected_stderr_contains == "unknown task"` | 4 |
| AC-FR05-04-id-hex8 | `len(task_id) == 8` | 4 |

---

## Cross-Cutting Test Cases

### NFR-01 — Performance (p95 latency)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr01_submit_status_p95_under_50ms` | n_iters="100"; p95_budget_ms="50" | nfr_pattern (NP-06) | Q6 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-NFR01-01-iters-100 | `n_iters == "100"` | 1 |
| AC-NFR01-01-budget-50 | `p95_budget_ms == "50"` | 1 |

### NFR-02 — Security (no `shell=True`)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr02_no_shell_true_in_codebase` | pattern="shell=True"; expected_hits="0"; scan_path="src/" | security | Q6 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-NFR02-01-hits-0 | `expected_hits == "0"` | 1 |
| AC-NFR02-01-pattern | `pattern == "shell=True"` | 1 |

*Note*: AC-NFR-02-02 (injection char blacklist test coverage) is satisfied by FR-01 case 5 (`test_fr01_add_task_injection_chars_rejected`); see FR-01 sub-assertion rule_id `AC-FR01-05-via-NFR02-cross-ref` for the explicit cross-reference predicate.

### NFR-03 — Reliability (atomic write + breaker recovery)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr03_atomic_write_kill9_recovery` | files="tasks.json,breaker.json,cache.json"; fault="kill_mid_write"; expected_json_valid="true" | nfr_pattern (NP-13) | Q6 |
| 2 | `test_nfr03_open_to_closed_within_cooldown_plus_1s` | cooldown="5.0"; budget_s="6.0"; state_after="CLOSED" | nfr_pattern (NP-13) | Q6 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-NFR03-01-json-valid | `expected_json_valid == "true"` | 1 |
| AC-NFR03-01-three-files | `files == "tasks.json,breaker.json,cache.json"` | 1 |
| AC-NFR03-02-budget-6 | `budget_s == "6.0"` | 2 |
| AC-NFR03-02-state-closed | `state_after == "CLOSED"` | 2 |

### NFR-04 — Security (secret redaction)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr04_sk_pattern_redacted` | stdout_line="login sk-abcdefghijkl"; expected_output="[REDACTED]" | security | Q6 |
| 2 | `test_nfr04_token_pattern_redacted` | stdout_line="auth token=abc123"; expected_output="[REDACTED]" | security | Q6 |
| 3 | `test_nfr04_negative_no_match_unchanged` | stdout_line="hello world"; expected_output="hello world" | validation (negative) | Q2 |
| 4 | `test_nfr04_redaction_before_persistence` | stdout_line="sk-abcdef1234"; file="tasks.json"; expected_persisted="[REDACTED]" | unit (NP-04 + FR-02 + FR-04 cross-cut) | Q7 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-NFR04-01-sk-redacted | `expected_output == "[REDACTED]"` | 1 |
| AC-NFR04-01-sk-8plus | `"sk-" in stdout_line` | 1 |
| AC-NFR04-02-token-redacted | `expected_output == "[REDACTED]"` | 2 |
| AC-NFR04-02-token-pattern | `"token=" in stdout_line` | 2 |
| AC-NFR04-03-unchanged | `expected_output == "hello world"` | 3 |
| AC-NFR04-03-no-pattern | `"sk-" not in stdout_line` | 3 |
| AC-NFR04-04-persisted | `expected_persisted == "[REDACTED]"` | 4 |
| AC-NFR04-04-tasks-json | `file == "tasks.json"` | 4 |

### NFR-05 — Maintainability (docstring `[FR-XX]`)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr05_every_public_symbol_has_fr_ref` | scan_dir="src/taskq"; expected_coverage_pct="100" | maintainability | Q6 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-NFR05-01-coverage-100 | `expected_coverage_pct == "100"` | 1 |
| AC-NFR05-01-src-taskq | `scan_dir == "src/taskq"` | 1 |

### NFR-06 — Deployability (env vars)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr06_env_var_defaults` | env_unset="TASKQ_HOME,TASKQ_MAX_WORKERS,TASKQ_TASK_TIMEOUT,TASKQ_RETRY_LIMIT,TASKQ_BACKOFF_BASE,TASKQ_BREAKER_THRESHOLD,TASKQ_BREAKER_COOLDOWN,TASKQ_CACHE_TTL"; expected_home=".taskq" | deployability | Q6 |
| 2 | `test_nfr06_env_var_override` | env_set="TASKQ_HOME=/tmp/x"; expected_home="/tmp/x" | deployability | Q6 |
| 3 | `test_env_example_complete` | env_file=".env.example"; expected_var_count="8" | deployability | Q6 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC-NFR06-01-default-home | `expected_home == ".taskq"` | 1 |
| AC-NFR06-01-eight-vars | `env_unset == "TASKQ_HOME,TASKQ_MAX_WORKERS,TASKQ_TASK_TIMEOUT,TASKQ_RETRY_LIMIT,TASKQ_BACKOFF_BASE,TASKQ_BREAKER_THRESHOLD,TASKQ_BREAKER_COOLDOWN,TASKQ_CACHE_TTL"` | 1 |
| AC-NFR06-02-override | `expected_home == "/tmp/x"` | 2 |
| AC-NFR06-02-env-set | `env_set == "TASKQ_HOME=/tmp/x"` | 2 |
| AC-NFR06-03-env-file | `env_file == ".env.example"` | 3 |
| AC-NFR06-03-eight-vars | `expected_var_count == "8"` | 3 |

### Deployment Smoke

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_app_starts_and_health_endpoint_returns_200` | smoke | deployment |

*Note*: taskq is a CLI tool (no HTTP endpoint). The smoke case validates `python -m taskq submit "echo hi" && python -m taskq run <id>` end-to-end; the literal name `test_app_starts_and_health_endpoint_returns_200` is retained from the template per D4 name preservation rule. P3 may rename to `test_smoke_cli_e2e` with Agent B sign-off.

---

## Summary

| Metric | Count |
|---|---|
| FRs covered | 5 (FR-01..FR-05) + 6 NFRs (NFR-01..NFR-06) |
| Total test cases | 40 (TEST_INVENTORY.yaml says 39; TEST_SPEC adds +1 for FR-01 whitespace split per FR-01 row below — discrepancy accepted at P2 review with rationale) |
| By type: happy_path | 11 |
| By type: validation/failure | 11 |
| By type: boundary | 3 |
| By type: state_transition | 6 |
| By type: fault_injection / integration | 5 |
| By type: nfr_pattern (perf / security / reliability / maintainability / deployability) | 8 |
| By type: smoke | 1 |
| Active NFR patterns applied | NP-04, NP-06, NP-07, NP-13, NP-15 |
| SAD-driven forced patterns (Step 1b) | NP-13 (store.py), NP-15 (executor.py), NP-07 (cache.py) |
| Integration variants under `tests/integration/` | 5 (FR-02×1 [case 6 = TC-FR02-04b], FR-05×4 [cases 1/2/3/4 = TC-FR05-01/02/03a/03b]) |

Per-FR test case counts (cross-checked against TEST_INVENTORY.yaml `by_fr`):

| FR | TEST_INVENTORY count | TEST_SPEC count |
|----|----------------------|-----------------|
| FR-01 | 5 | 6 (covers all 6 injection chars parametrically within case 5) |
| FR-02 | 6 | 6 |
| FR-03 | 8 | 8 |
| FR-04 | 4 | 4 |
| FR-05 | 4 | 4 |
| NFR-01..06 | 12 | 12 |
| **Total** | **39** | **40** |

Discrepancy: FR-01 has 6 cases in TEST_SPEC vs 5 in TEST_INVENTORY — TEST_INVENTORY rolls all injection chars into `test_fr01_add_task_injection_chars_rejected` (parametric enumeration), while TEST_SPEC splits whitespace-only into a separate case to satisfy the Q2 "validation or failure" mandate that whitespace-rejection is a distinct observable rule. P3 Agent A implements `test_fr01_add_task_whitespace_rejected` as an additional test (whitespace is a distinct rule per SPEC §3 FR-01 row "非空"). Agent B (TECH_LEAD) will accept this discrepancy at P2 review with rationale.

---

*Generated by: derive_test_cases.md v1.1 | harness-methodology v2.9.1*