# TEST_SPEC.md — Per-FR Test Catalog (Single Source of Truth)

> **Phase 2 deliverable.** Generated by ARCHITECT (Agent A) for the taskq project.
> Test names from P1 `TEST_INVENTORY.yaml` v1.5.0 are preserved verbatim where
> specified. This is the SINGLE SOURCE OF TRUTH for all test traceability at
> Gates 1–4 (v2.9.1 B.3 unified D4). Validated by TECH_LEAD (Agent B) before
> P2 exit. P3 Agent A implements tests FROM this catalog — not ad-hoc.
>
> Self-consistency is locked HERE, before P3, by the
> `harness_cli.py check-test-spec-consistency` gate; unsatisfiable sub-assertion
> groups (predicate false for declared Inputs / contradictory length facts) fail
> P2 and block P3 from implementing a contradictory spec.

**Project**: taskq
**SPEC version**: SPEC.md v2.0.0 (2026-06-15)
**SAD version**: SAD.md v1.0.0 (2026-06-29)
**TEST_INVENTORY.yaml version**: v1.5.0 (2026-06-29)
**Generated**: 2026-06-29
**Active NFR Patterns**: NP-04 (validation), NP-06 (latency SLA), NP-13 (concurrency), NP-15 (timeout)
**Language / test_runner**: Python 3.11 (pytest, per `CLAUDE.md` Language)

---

## NFR Pattern Activation (Step 1 + Step 1b Output)

Step 1 — SRS/SPEC keyword scan (NP activated only when keyword present).
Step 1b — Architecture-risk scan (SAD module → forced NP regardless of keyword;
the forced cases must live under `tests/integration/`).

| Pattern | Trigger (SRS keyword / SAD module) | Activated |
|---------|-------------------------------------|-----------|
| NP-01 (auth 401)            | not found                       | ❌ |
| NP-02 (authz 403)           | not found                       | ❌ |
| NP-03 (rate limit 429)      | not found                       | ❌ |
| NP-04 (validation 422)      | SPEC §3 FR-01 rules (non-empty / length / injection blacklist → exit 2) | ✅ |
| NP-05 (idempotency)         | not found                       | ❌ |
| NP-06 (latency SLA)         | SPEC §4 NFR-01 ("submit + status … p95 < 50ms") | ✅ |
| NP-07 (dependency fault)    | not found (no remote cache / no external service) | ❌ |
| NP-08 (security attack)     | not found                       | ❌ |
| NP-09 (audit log)           | not found                       | ❌ |
| NP-10 (data round-trip)     | SPEC §3 FR-01 (load → submit → save → load round-trip) | ✅ |
| NP-11 (backward compat)     | not found                       | ❌ |
| NP-12 (pagination)          | not found (list emits all tasks, no pagination) | ❌ |
| NP-13 (concurrency)         | SAD: `io/store.py` (shared mutable JSON state via `save_tasks_atomic`; concurrent readers/writers around the rename) | ✅ |
| NP-14 (encryption)          | not found                       | ❌ |
| NP-15 (timeout)             | SAD: `runner/runner.py` (`subprocess.run(... timeout=cfg.task_timeout)`; single external-process call site) | ✅ |

**Forced-by-architecture cases (Step 1b)**: NP-13 and NP-15 are forced by SAD module shape, not by SRS keyword. Per protocol they MUST materialise as integration tests under `tests/integration/` (see Cross-Cutting § Integration below). NP-04 and NP-06 / NP-10 are SRS-keyword-driven and materialise as unit tests under their owning FR.

---

## Functional Requirement Test Cases

### FR-01: Task model, validation, persistence (`taskq submit`)

**Classification**: DATA_ENTITY (Task record shape) + INTEGRATION (validation→store→atomic write chain)
**Active Patterns**: NP-04, NP-10, NP-13

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr01_001_empty_command_rejected` | command=""; cmd_type="empty" | validation | Q2 |
| 2 | `test_fr01_001_empty_command_rejected` | command="   "; cmd_type="whitespace" | validation | Q2 |
| 3 | `test_fr01_002_length_exceeds_1000_rejected` | length=1001; cmd_type="over_limit" | boundary | Q3 |
| 4 | `test_fr01_002_length_exceeds_1000_rejected` | length=1000; cmd_type="at_limit" | boundary | Q3 |
| 5 | `test_fr01_003_injection_chars_rejected` | inj_char=";"; cmd="echo a;b" | validation | Q2 |
| 6 | `test_fr01_003_injection_chars_rejected` | inj_char="\|"; cmd="echo a\|b" | validation | Q2 |
| 7 | `test_fr01_003_injection_chars_rejected` | inj_char="&"; cmd="echo a&b" | validation | Q2 |
| 8 | `test_fr01_003_injection_chars_rejected` | inj_char="$"; cmd="echo a$b" | validation | Q2 |
| 9 | `test_fr01_003_injection_chars_rejected` | inj_char=">"; cmd="echo a>b" | validation | Q2 |
| 10 | `test_fr01_003_injection_chars_rejected` | inj_char="<"; cmd="echo a<b" | validation | Q2 |
| 11 | `test_fr01_003_injection_chars_rejected` | inj_char="`"; cmd="echo a`b" | validation | Q2 |
| 12 | `test_fr01_004_pending_fields_initialized` | cmd="echo hi"; cmd_type="valid" | happy_path | Q1 |
| 13 | `test_fr01_005_atomic_write_survives_interrupt` | concurrent_readers=4; iterations=200 | fault_injection | Q5 |
| 14 | `test_fr01_006_corrupt_store_exit1` | store_payload="{not valid json" | fault_injection | Q5 |

**Sub-assertions** — predicate over a case's declared Inputs / the production `result`. Predicates are spec-level declarations evaluated by the P2 self-consistency engine.

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR01-bounded-len-reject-over-limit | `length > 1000` | 3 |
| FR01-bounded-len-accept-at-limit | `length == 1000` | 4 |
| FR01-injection-char-present | `inj_char in cmd` | 5, 6, 7, 8, 9, 10, 11 |
| FR01-empty-or-whitespace | `command.strip() == ""` | 1, 2 |
| FR01-valid-has-no-forbidden | `inj_char not in cmd` | 12 |

---

### FR-02: Task execution, state machine, retry (`taskq run`)

**Classification**: STATE_MACHINE (pending → running → done | failed | timeout) + INTEGRATION (runner ⇄ store via callback)
**Active Patterns**: NP-15, NP-13

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr02_001_subprocess_form_no_shell` | cmd="echo hi" | happy_path | Q1 |
| 2 | `test_fr02_002_state_transitions` | exit_code=0; cmd="true" | state_transition | Q4 |
| 3 | `test_fr02_002_state_transitions` | exit_code=1; cmd="false" | state_transition | Q4 |
| 4 | `test_fr02_002_state_transitions` | sleep_seconds=15; cmd="sleep 15"; timeout=1.0 | state_transition | Q4 |
| 5 | `test_fr02_003_result_fields_populated` | stdout_text="abc"; cmd="echo abc" | happy_path | Q1 |
| 6 | `test_fr02_004_retry_until_limit` | retry_limit=2; cmd="false" | fault_injection | Q5 |
| 7 | `test_fr02_004_retry_until_limit` | retry_limit=0; cmd="false" | boundary | Q3 |
| 8 | `test_fr02_004_retry_until_limit` | retry_limit=3; cmd="false" | boundary | Q3 |
| 9 | `test_fr02_004b_no_retry_on_done` | cmd="true" | happy_path | Q1 |
| 10 | `test_fr02_005_timeout_exit_code_4` | sleep_seconds=15; cmd="sleep 15"; timeout=1.0; single_task_mode=true | fault_injection | Q5 |
| 11 | `test_fr02_006_unexpected_exception_exit1` | cmd="true" | fault_injection | Q5 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR02-done-when-exit-zero | `exit_code == 0` | 2, 9 |
| FR02-failed-when-exit-nonzero | `exit_code != 0` | 3, 6, 7, 8 |
| FR02-timeout-when-exceeds | `sleep_seconds > timeout` | 4, 10 |
| FR02-single-task-mode-flags-timeout | `single_task_mode == True` | 10 |
| FR02-tail-truncation-bounded | `len(result.stdout_tail) <= 2000` | 5 |

---

### FR-03: CLI surface, query, exit codes (`taskq submit|run|status|list|clear`)

**Classification**: API_ENDPOINT (argparse-driven CLI dispatch)
**Active Patterns**: NP-04

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr03_001_subcommands_dispatch` | subcmd="submit"; cmd="echo hi" | happy_path | Q1 |
| 2 | `test_fr03_001_subcommands_dispatch` | subcmd="list"; tasks_present=0 | happy_path | Q1 |
| 3 | `test_fr03_001b_status_unknown_id_exit2` | task_id="deadbeef" | validation | Q2 |
| 4 | `test_fr03_002_json_flag_single_line` | subcmd="status"; task_id="abcdef12"; json_flag=true | happy_path | Q1 |
| 5 | `test_fr03_003_exit_code_matrix` | cmd=""; cmd_type="empty" | boundary | Q3 |
| 6 | `test_fr03_003_exit_code_matrix` | injection_char="\|"; cmd="echo a\|b" | validation | Q2 |
| 7 | `test_fr03_003_exit_code_matrix` | store_payload="{not valid json" | fault_injection | Q5 |
| 8 | `test_fr03_003_exit_code_matrix` | sleep_seconds=15; cmd="sleep 15"; timeout=1.0; subcmd="run" | fault_injection | Q5 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR03-list-empty-shows-zero | `tasks_present == 0` | 2 |
| FR03-json-single-line-no-newline | `"\n" not in result.stdout` | 4 |
| FR03-unknown-id-fmt | `len(task_id) == 8` | 3 |
| FR03-timeout-in-single-task | `sleep_seconds > timeout` | 8 |
| FR03-empty-cmd-rejected | `cmd == ""` | 5 |
| FR03-injection-in-cmd | `injection_char in cmd` | 6 |

---

### NFR-01: Performance — submit + status p95 < 50ms (100 iter, fork-less)

**Classification**: INFRASTRUCTURE (cross-cutting benchmark)
**Active Patterns**: NP-06

**Measurable target (binding)**: `target_ms=50` (p95 latency ceiling, in milliseconds), `sample_size=100` (iteration count for percentile estimation), `pair="submit+status"` (excludes subprocess; NP-15 fork-free constraint). All three fields are measurable Inputs in the test harness; deviation fails Gate 4 D14.

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `perf_fr03_nfr01_p95_under_50ms` | iterations=100; pair="submit+status"; target_ms=50; sample_size=100 | nfr_pattern | Q6 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR01-iter-count-positive | `iterations > 0` | 1 |
| NFR01-excludes-subprocess | `pair != "submit+run"` | 1 |
| NFR01-target-ms-positive | `target_ms > 0` | 1 |
| NFR01-sample-size-positive | `sample_size > 0` | 1 |
| NFR01-target-equals-50ms | `target_ms == 50` | 1 |
| NFR01-sample-equals-100 | `sample_size == 100` | 1 |

---

### NFR-02: Security — no `shell=True`; blacklist coverage

**Classification**: SECURITY_CONTROL (static + behavioural coverage)
**Active Patterns**: NP-04

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | (static grep `shell=True` over `src/taskq/`) | source_dir="src/taskq" | security_control | Q6 |
| 2 | (7-char behavioural coverage via `test_fr01_003_injection_chars_rejected`) | chars=7 | security_control | Q6 |

> NP-02 entry: there is no remote cache / external service in this project (NFR-02 owns "single subprocess call site"). Hooked to FR-01 case 5–11 + FR-02 case 1 by TEST_INVENTORY cross_ref_nfrs.

---

### NFR-03: Reliability — atomic JSON write + secret-line redaction

**Classification**: INTEGRATION (filesystem-coupled atomic write + redaction filter)
**Active Patterns**: NP-13, NP-15

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_nfr03_001_atomic_write_sigkill_chaos` (P4 chaos fixture) | mode="sigkill_mid_write"; home_tmp="…" | chaos | Q6 |
| 2 | `test_nfr03_002_secret_line_redacted` | line="sk-abcdef1234567890 extra" | happy_path | Q1 |
| 3 | `test_nfr03_002_secret_line_redacted` | line="token=abcdef1234567890" | happy_path | Q1 |
| 4 | `test_nfr03_002_secret_line_redacted` | line="benign output" | happy_path | Q1 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR03-secret-line-sk-prefix | `line.startswith("sk-")` | 2 |
| NFR03-secret-line-token-prefix | `line.startswith("token=")` | 3 |
| NFR03-bypass-control | `"sk-" not in line and "token=" not in line` | 4 |

---

## Cross-Cutting Test Cases

### NFR Integration (system-wide, not tied to a single FR)

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_atomic_write_no_partial_json_observed` (NP-13 forced; SAD: `io/store.py` shared mutable state) | nfr_pattern | Step 1b |
| 2 | `test_subprocess_timeout_watchdog_does_not_hang_runner` (NP-15 forced; SAD: `runner/runner.py` external process) | nfr_pattern | Step 1b |

> Both forced cases live under `tests/integration/` per the Step 1b protocol.

### Backward Compatibility

> Not applicable — taskq is a single-phase deliverable, no prior-version contract to preserve.

### Deployment Smoke

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_app_starts_and_cli_help_returns_0` | smoke | deployment |

---

## Summary

| Metric | Count |
|---|---|
| FRs covered | 3 (FR-01, FR-02, FR-03) + NFR-01, NFR-02, NFR-03 |
| Total test cases | 38 (per TEST_INVENTORY.yaml v1.5.0: 19 pytest + 1 benchmark + NFR-02 static + chaos; per-FR rows above are parametrize expansions of the 19 named tc_ids) |
| By type: happy_path | 6 (FR-01 case 12; FR-02 case 5, 9; FR-03 case 1, 2, 4) |
| By type: validation/failure | 14 (FR-01 cases 1, 2, 5–11; FR-03 cases 3, 6) |
| By type: boundary | 5 (FR-01 cases 3, 4; FR-02 cases 7, 8; FR-03 case 5) |
| By type: state_transition | 3 (FR-02 cases 2, 3, 4) |
| By type: fault_injection | 5 (FR-01 cases 13, 14; FR-02 cases 6, 10, 11; FR-03 cases 7, 8) |
| By type: nfr_pattern | 3 (NFR-01 case 1; Cross-Cutting cases 1, 2) |
| By type: security_control | 2 (NFR-02 cases 1, 2) |
| By type: chaos | 1 (NFR-03 case 1) |
| Active NFR patterns applied | NP-04, NP-06, NP-10, NP-13, NP-15 |

---

*Generated by: derive_test_cases.md protocol | harness-methodology v2.9.1 (B.3)*
