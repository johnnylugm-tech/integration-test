[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "taskq"
version = "0.1.0"
description = "Minimal task queue CLI — FR-01"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["taskq"]