
# harness-methodology runtime (auto-added by git_strategy)
# .sessi-work/tmp/ — J: convention sandbox for LLM agent debug artifacts
# (debug_*.py, coverage_*.html, exploration scripts). All workflow JS
# SCOPE RULES instruct agents to write here, not to source tree.
.sessi-work/
.methodology/last_block.md
.methodology/steering_history.json
.venv/
venv/
ENV/
.taskq/        # taskq runtime data dir (SPEC §5.1 TASKQ_HOME; tasks/breaker/cache .json)
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.mutmut-cache/
.code-review-graph/
.methodology/.state.lock
.methodology/trace/attestation.latest.json
# Pure debug/trace logs — read only from the working tree, never from git
# history; tracking them left a perpetually-dirty tree (gate_timestamps.jsonl
# stays tracked as functional FR-gate state).
.methodology/sessions_spawn.log
.methodology/sessions_spawn.log.lock
.harness/traces/
