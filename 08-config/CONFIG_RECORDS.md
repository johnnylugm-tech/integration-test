# CONFIG_RECORDS.md - integration-test

> On-demand Lazy Load template.

## 1. Version Information
- Version: vgate4-20260703-score96-8-g8f5211c
- Git Commit: 8f5211c
- Release Date: 2026-07-03

## 2. Runtime Configuration
| Environment | Config |
|-------------|--------|
| Development | {{config}} |
| Production | {{config}} |

## 3. Dependency List
```
{{pip freeze / npm lock output}}
```

## 4. Environment Variables
| Variable | Type | Description |
|----------|------|-------------|
| {{VAR}} | secret | {{description}} |

## 5. Deployment Log
| Date | Version | Method | Executor |
|------|---------|--------|----------|
| 2026-07-03 | gate4-20260703-score96-8-g8f5211c | {{method}} | {{name}} |

## 6. Configuration Change Log
| Phase | Change | Rationale |
|-------|--------|----------|
| Phase 8 | {{change}} | {{reason}} |

## 7. Rollback SOP
**Trigger Condition**: {{condition}}
**Commands**:
```bash
{{rollback commands}}
```

## 8. Configuration Compliance
- [ ] Phase 7 risk mitigations implemented
- [ ] Monitoring thresholds configured
- [ ] Circuit breaker enabled

## Human Context (P8 append)

> Human-authored context appended to the framework-generated baseline. Do NOT regenerate above sections; this block supplements ownership, rotation, and audit info.

### Config Item Ownership
| Config Item | Owner | Contact | Source-of-Truth |
|-------------|-------|---------|-----------------|
| `taskq.executor` runtime tunables (concurrency, timeout, retry) | taskq-platform team | #taskq-platform on Slack | `taskq/executor.py` module defaults (see Section 4 env-var inventory) |
| `taskq.store` connection / pool config | taskq-platform team | #taskq-platform on Slack | `taskq/store.py` module defaults |
| Secrets (DB URL, broker creds, signing key) | SRE / secrets rotation bot | sre-oncall@example.internal | External secrets manager (e.g. Vault / KMS) — referenced by env-var inventory above |
| Feature flags (kill-switch, experiment toggles) | product-eng on-call | product-oncall@example.internal | Feature-flag service (config keys listed in Section 4) |
| Monitoring thresholds / circuit-breaker tuning | SRE | sre-oncall@example.internal | `taskq/executor.py` + observability config |

### Secret Rotation Cadence
| Secret Class | Rotation Period | Owner | Procedure |
|--------------|-----------------|-------|-----------|
| Database credentials | 90 days | SRE | Rotate via secrets manager; rolling restart of `taskq.executor` workers; verify with health check |
| Broker / queue credentials | 90 days | SRE | Rotate via secrets manager; drain and reconnect `taskq.store` |
| Signing / HMAC keys | 180 days | security | Dual-key rotation (old + new overlap window = 7 days) |
| API tokens / external service keys | 60 days | service owner | Per-service rotation runbook (link in release checklist) |
| On-call / pager credentials | 30 days | SRE | Forced rotation after any on-call handoff |

### Access Audit Log Reference
| Audit Surface | Log Location | Retention | Reviewer |
|---------------|--------------|-----------|----------|
| Secrets access | secrets manager audit log (`sre-audit-bucket`) | 365 days | SRE lead (monthly review) |
| Config file writes | Git audit log on `main` branch (`git log -- <path>`) | indefinite (git) | Code owners via CODEOWNERS |
| Feature-flag toggles | feature-flag service audit log | 180 days | product-eng on-call |
| Production env-var changes | deploy pipeline audit log (`deploy-audit/`) | 365 days | SRE |
| Access to `08-config/` artifacts | GitHub audit log (org-level) | indefinite | Repo admin |

### Notes
- Framework-generated sections (1-8) above this block are deterministic outputs of `harness/scripts/phase8_doc_gen.py` and MUST be regenerated by the harness, not edited by hand.
- This Human Context block is updated only when ownership, rotation cadence, or audit destinations change — treat as living doc maintained by the listed owners.
